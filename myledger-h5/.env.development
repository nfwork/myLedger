# 开发时走 Vite 代理：保持为空，请求发往当前页面同源（如 http://192.168.0.156:5173/api/...），由 Vite 转发到后端，避免跨域。
# 若填成 http://192.168.0.156:8080 等直连后端，浏览器会跨域，需后端 WebCorsConfig 放行对应 Origin。
VITE_API_BASE=

# 后端地址（仅给 vite.config.cjs 的 proxy target 用，不是浏览器里的 API 根）
VITE_DEV_PROXY_TARGET=http://192.168.0.156:8080
