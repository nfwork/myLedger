---
description: 项目通用 — 重大变更落档，便于后续维护
alwaysApply: true
---

# 变更落档（项目通用）

**重大**技术改动（数据模型、对外 API 契约、部署与安全策略）或业务规则反转时，须在版本说明中留下可追溯记录，不得仅留在对话或口头约定里。

## 记录位置

- 主文件：`docs/版本说明.md`
- 在同文件内 **追加**（建议按时间倒序或统一在「变更记录」小节自下而上），每条尽量简短：日期、类型（`技术` / `业务`）、一句话摘要、必要的迁移说明。

## README（`README.md`）

当改动涉及 **技术栈/架构、核心模块边界、主数据模型、对外 API、运行与部署** 时，除版本说明外，应同步更新 **`README.md`** 或 **`docs/业务归档.md`** 中对应段落，使文档与仓库一致。单点 bugfix 可不强制改 README。

## 对 Agent 的要求

在完成用户请求的代码或方案修改后，若属上述重大变更，**顺手更新** `docs/版本说明.md`（除非用户明确说本次不必记录），并按需更新 README / 业务归档。不要新建多套互斥的变更日志；有冲突时以 **`docs/版本说明.md`** 为项目内统一入口。
