---
description: myLedger 后端 — dbfound 工程约定（路径、命名、与框架规则叠加）
globs:
  - "**/model/**/*.xml"
  - "**/application*.yml"
  - "**/*Adapter*.java"
  - "**/*Controller*.java"
  - "**/*Service*.java"
alwaysApply: false
---

# myLedger 与 dbfound 相关的工程约定

与 **`.cursor/rules/dbfound-framework.mdc`**（纯框架语法）配合使用；本文件只写本仓库路径、模块划分与列名/JavaBean 习惯。

## Model 目录与 `model_name`

业务 model 位于 **`myledger-api/src/main/resources/model/`**，按模块分子目录，路径即 **`model_name`**（无 `.xml` 后缀）：

| 路径示例 | `model_name` | 说明 |
|---|---|---|
| `model/user/user.xml` | `user/user` | 用户 |
| `model/ledger_settings/account.xml` | `ledger_settings/account` | 资金账户 |
| `model/ledger_settings/category.xml` | `ledger_settings/category` | 分类 |
| `model/bookkeeping/entry.xml` | `bookkeeping/entry` | 流水 |
| `model/report/summary.xml` | `report/summary` | 报表统计 |

前端 `myledger-h5` 的 `src/api` 与上述目录对齐时，见 **`.cursor/rules/myledger-h5-api-urls.mdc`**（完整 URL、禁止抽离公共前缀）。

## Model HTTP 白名单

- dbfound Model HTTP 已通过 **`myledger-api/src/main/resources/application.yml`** 的 **`dbfound.web.api-allow-urls`** 启用显式白名单；未列出的 `*.query` / `*.execute` 默认不允许终端访问。
- 后续新增或改名某个 model 的 `query` / `execute` 时，若该接口需要给 H5、Android、curl 或其它终端通过 HTTP 访问，必须同步把完整路径加入 **`api-allow-urls`**，例如 `"/bookkeeping/entry.query!list"`。
- 仅服务端 Java 通过 **`ModelExecutor`** 内部调用的接口不要加入 HTTP 白名单，例如登录校验、refresh token 维护等内部 model API。

## 列名与强类型映射

- SQL 与 dbfound **HTTP 查询 JSON** 的字段名与库表一致，采用 **snake_case**。
- **`ModelExecutor.query*` 映射到 `Class`（JavaBean）时**，下划线列名与驼峰属性（如 `user_id` → `userId`）由框架**默认支持**，实体按 **标准驼峰** 编写即可，**不必**为 JavaBean 去开 **`dbfound.system.underscore-to-camel-case`**（该配置面向 **Map 等非 Bean 结果**里键名是否由下划线转驼峰，与强类型 Bean 无关）。
- 对外 REST 的 JSON 约定集中在 **`myledger-api/.../config/JacksonJsonConfig.java`**（snake_case、NON_NULL 等），DTO 上不堆 Jackson 注解。
